kind = "web"
previewPath = "/"
title = "Sky Official"
version = "1.0.0"
id = "artifacts/sky-official"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24534

[services.development]
run = "pnpm --filter @workspace/sky-official run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sky-official", "run", "build" ]
publicDir = "artifacts/sky-official/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24534"
BASE_PATH = "/"
